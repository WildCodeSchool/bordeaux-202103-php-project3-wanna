<form method="post" action="{{ path('tchat_delete', {'id': tchat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tchat.id) }}">
    <button class="btn">Delete</button>
</form>
