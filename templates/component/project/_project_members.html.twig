<h3>They are currently contributing to this project</h3>
<ul>
    {% for participant in project.participants %}
        <li>{{ participant.role }} - {{ participant.user.firstname }}</li>
    {% endfor %}
</ul>
